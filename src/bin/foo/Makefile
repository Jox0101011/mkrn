# arquivo de exemplo
# dominio publico

# nome do programa
PROG=   foo

# arquivos de codigo fonte (se tiver outro alem de ${PROG})
SRCS=   foo.c

# incluir o template
include prog.mk

# cflags opcionais
CFLAGS+= -Wall -pedantic -std=c89 -Wextra
